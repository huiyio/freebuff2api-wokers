# 安全策略

## 报告安全问题

请不要在公开 Issue、Pull Request、聊天或截图中提交 Token、密码、代理认证信息、数据库、完整请求体或个人数据。

优先使用 GitHub 的私密漏洞报告入口：[提交私密报告](https://github.com/huiyio/freebuff2api-wokers/security/advisories/new)。如果该入口在你的账号或仓库中不可用，请先通过维护者的私密渠道联系，并只提供脱敏复现步骤。不要为了证明问题而访问或导出他人的账号。

报告应包含：

- 受影响的 commit、镜像 digest 或部署版本；
- 影响范围、复现条件和最小复现步骤；
- 是否已经在生产环境发生，以及已采取的隔离措施；
- 脱敏后的状态码、时间、请求 ID 和日志片段。

## 维护者处理原则

维护者会先确认报告范围，按 [CHANGE_CONTROL.md](CHANGE_CONTROL.md) 分级，必要时私下协调修复、缓解、公告和版本发布。未获得报告者同意前，不公开可用于利用的细节或个人信息。

该仓库不承诺固定响应时间或安全 SLA；面向客户的响应时间必须由部署运营方另行书面约定。

## 生产环境凭据事件

部署运营方发现秘密泄露或疑似未授权访问时，应立即：

1. 停止暴露的管理端/API 或限制来源；
2. 撤销并重新生成 Freebuff Token、API Key、代理凭据、管理员会话和相关 GitHub/Cloudflare 凭据；
3. 保留脱敏审计日志、时间线和受影响版本，不要覆盖证据；
4. 检查 SQLite、备份、镜像层、CI 日志、shell 历史和浏览器剪贴板；
5. 评估通知用户、上游服务方、监管机构或其他合同方的义务；
6. 记录根因、修复人、批准人和恢复时间。

维护者不会替运营方保管或恢复生产秘密，也不会要求用户把秘密粘贴到 Issue 或对话中。

## 安全边界

代理、账号轮换和协议兼容层不应被解释为绕过封禁、风控、地域限制或付费限制的安全功能。部署者必须在自己的网络、账号和数据授权范围内验证配置。
