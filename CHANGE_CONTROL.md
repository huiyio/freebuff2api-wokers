# 变更、发布与回滚流程

更新日期：2026-08-15

本流程适用于 Worker、Node/Docker 适配层、管理端、数据库迁移、部署配置和文档中会改变使用责任的修改。它的目标是让“谁提出、谁审核、谁批准、谁上线、谁回滚”可追溯。

## 1. 变更分级

| 等级 | 示例 | 最低要求 |
|---|---|---|
| P0 | 凭据泄露、未授权访问、数据损坏、错误路由到他人账号 | 立即停止扩散；指定事件负责人；私下记录和修复；必要时撤销秘密 |
| P1 | API/认证行为变化、代理路由变化、数据库迁移、上游协议变化 | 代码审查、回归测试、部署运营方批准、备份和明确回滚方案 |
| P2 | UI、文档、日志和非行为性重构 | 至少一名审阅者；相关链接/测试通过；记录用户可见变化 |

若无法确定等级，按较高等级处理。安全和隐私影响优先于发布速度。

## 2. 标准流程

1. **提出**：写明目的、范围、影响角色、数据流变化、风险、负责人和回滚方式。
2. **核对上游**：检查当前远程基线、上游 API/许可证/依赖变化，不整文件覆盖本地安全修复。
3. **实现**：在功能分支修改；不把秘密写入代码、日志、Issue、测试快照或镜像层。
4. **验证**：运行 `npm.cmd test`、`npm.cmd run check`、`npm.cmd audit --omit=dev` 和 `git diff --check`；涉及 UI 时做桌面/手机检查。
5. **文档**：同步 README、责任矩阵、迁移/回滚说明和已知限制；涉及对外服务时更新运营方条款和隐私文件。
6. **审阅批准**：至少一名非作者审阅；生产变更还需要部署运营方负责人批准。未批准的分支不得作为生产版本。
7. **发布验收**：记录 commit、镜像 digest、配置版本、健康检查、最小业务请求和监控观察窗口。
8. **关闭**：记录实际结果、异常、后续任务和责任移交；失败时按回滚方案处理。

## 3. 不可省略的生产检查

- API Key、管理员凭据、主密钥和代理密码来自安全注入，不在命令历史和日志中出现；
- `ADMIN_COOKIE_SECURE`、反向代理和管理端网络范围与实际拓扑一致；
- SQLite 备份可读，`ACCOUNT_STORE_KEY` 的保管人和恢复步骤已确认；
- 启用账号的代理、上游资格和测试请求均获得授权；
- 版本不是浮动的 `latest`，或已明确记录运行时更新策略和校验值；
- 变更不会把 `FREEBUFF_TOKEN` 兼容旁路与 Web 管理账号库同时启用；
- 任何对第三方的可用性、额度、退款或数据处理承诺都由运营方书面确认。

## 4. 回滚规则

发生 P0、数据损坏、认证绕过、持续 5xx 或明显上游协议不兼容时，运营方可先回滚到最近一个验收通过的 commit/镜像，并保留故障版本、脱敏日志和数据库备份。回滚不等于删除证据，也不等于恢复已被上游撤销的账号或额度。

数据库迁移必须提供向后兼容或明确的备份恢复路径；禁止用删除生产数据库来“修复”配置或改变管理员身份。

## 5. 记录模板

```text
变更编号/标题：
等级：P0 / P1 / P2
提出人 / 审阅人 / 批准人 / 上线人：
涉及版本和文件：
影响角色与数据：
测试证据：
部署时间与环境：
回滚版本和恢复点：
结果、异常和后续任务：
```
