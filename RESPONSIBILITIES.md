# 责任划分与交付边界

更新日期：2026-08-15

> 本文件用于项目协作、部署交接和争议预防。它不是单方服务合同；对外提供服务时，应把适用条款、联系人、服务级别和赔偿范围写入双方签署的合同。

## 1. 角色定义

| 角色 | 含义 | 默认责任主体 |
|---|---|---|
| 维护者 | 维护本 Fork、源代码、文档、测试和发布分支的人或组织 | 以仓库信息和书面交接记录为准 |
| 部署运营方 | 持有服务器、域名、Docker、Cloudflare、数据库和生产凭据的一方 | 部署本项目的个人或组织 |
| 调用方 | 使用 API、管理端或 SDK 的个人、团队和下游应用 | API 使用者或其雇主 |
| 上游服务方 | Freebuff/Codebuff、模型提供方、Cloudflare、GitHub、Docker、代理商等外部服务 | 各服务提供商 |

同一主体可以同时承担多个角色；角色重合不等于责任自动转移。交接时应记录人员、组织、时间和版本。

## 2. 工作项主责矩阵

“主责”表示必须完成或明确拒绝该事项；“协作”表示应提供信息；“知会”表示应能收到结果。

| 工作项 | 维护者 | 部署运营方 | 调用方 | 上游服务方 |
|---|---|---|---|---|
| 源代码、测试和版本记录 | 主责 | 知会 | 知会 | 不适用 |
| 部署参数、域名、端口和防火墙 | 协作 | 主责 | 知会 | 协作 |
| 账号、Token、代理和 API Key 的取得与保管 | 说明 | 主责 | 不得索取未授权凭据 | 上游负责账号状态 |
| 管理员账号、SQLite、主密钥和备份 | 说明 | 主责 | 不适用 | 不适用 |
| 请求数据、个人信息、日志和保留期限 | 说明 | 主责 | 提供合法数据并遵守政策 | 按其条款处理 |
| API 客户端兼容和调用频率 | 说明已知限制 | 主责 | 主责 | 可随时改变接口 |
| 模型、额度、封禁和服务可用性 | 记录已知风险 | 监控和处置 | 接受上游规则 | 主责 |
| 安全漏洞响应 | 接收和修复代码问题 | 立即隔离生产风险 | 私下报告、不得公开秘密 | 处理其平台问题 |
| 发布、升级、回滚和变更审批 | 提供变更内容 | 主责是否上线 | 配合验收 | 平台变更不受项目控制 |
| 对外承诺、收费、退款和客户支持 | 不默认承担 | 主责 | 按合同联系运营方 | 按其服务条款 |

## 3. 维护者交付范围

维护者负责在已声明范围内：

- 保留源代码许可证、第三方归属和已知限制；
- 对合并到受保护分支的代码运行约定的静态检查和测试；
- 记录有意的行为变化、配置变化、迁移和回滚方法；
- 对收到的安全报告进行分级，并在能力范围内发布修复或缓解说明；
- 不把任何真实账号、Token、代理密码、生产数据库或客户数据作为交付物。

维护者不负责部署者的服务器安全、秘密保管、上游账号资格、代理合法性、客户数据处理、第三方服务 SLA 或未经过维护者审查的改版。

## 4. 部署运营方交付范围

生产上线前，运营方必须指定一名负责人和一名备份负责人，并完成：

1. 确认账号、Token、代理和数据处理均有授权；
2. 记录部署版本、镜像摘要、域名、端口、秘密来源和访问人员；
3. 完成管理员登录、API Key、代理、`/healthz`、模型列表和最小对话验收；
4. 备份 SQLite 与 `ACCOUNT_STORE_KEY`，验证至少一次恢复；
5. 限制管理端访问、配置 HTTPS、设置日志和告警留存期限；
6. 明确谁有权批准升级、停机、回滚、删除账号和撤销凭据。

## 5. 交接与争议证据

每次交接至少保存以下非秘密记录：

```text
项目/环境：
责任组织与负责人：
部署时间（含时区）：
Git commit / 镜像 digest：
配置与秘密交接方式（不记录秘密内容）：
验收命令与结果：
备份位置与恢复演练时间：
回滚版本：
已知限制与未完成事项：
审批人/接收人：
```

不要把 Token、密码、代理 URL 中的认证信息、个人数据或完整请求体写入工单、截图、聊天或 Git。争议调查只提交脱敏后的时间、版本、状态码、请求 ID 和日志片段。
